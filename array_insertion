#include <iostream>
using namespace std; 

int main(){

  cout << "Insert the length of the array - " << flush; 
  int n; 
  int ar[n];
  cin >> n; 
  
  for (int i=0; i <= sizeof(ar[n]/ar[o]); i++){
    cin >> a[i]; 
    }


for (int i=0; i <= sizeof(ar[n]/ar[o]); i++){
    cout << a[i] << " " << flush;
    }

}
